{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm run dev:serve",
            "name": "Run tailwind + host server",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
